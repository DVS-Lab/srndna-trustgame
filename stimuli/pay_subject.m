function pay_subject(subnum)
% This function will convert the output to BIDS.
% The convert*BIDS functions display how much to pay for each run.

%convertSharedReward_BIDS(subnum);
convertTrust_BIDS(subnum);
%convertUG_BIDS(subnum);



